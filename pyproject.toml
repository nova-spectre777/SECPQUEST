[project]
name = "secpquest"
version = "0.1.0"
description = "RANGE-Ω public Bitcoin puzzle mathematics and verifiable search engine"
requires-python = ">=3.11"
authors = [{name = "Telegram @intelwire"}]
license = {text = "MIT"}

[project.scripts]
secpquest = "secpquest.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["secpquest"]
